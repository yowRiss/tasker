# Public by design: the Vue app uses this for its API base URL.
# Empty means same-origin. Vite proxies /v1 to Go during development and the
# production Go binary serves both the app and API from one origin.
VITE_API_BASE_URL=
