DATABASE_URL=postgresql://app_api:password@host:6543/postgres?sslmode=require
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=your-server-only-secret-key
JWT_SECRET=your-32-byte-secret-key-change-in-production
# Required only when the frontend is served from another origin (development).
# Leave unset in the same-origin embedded production deployment.
CORS_ALLOWED_ORIGIN=http://localhost:5173
API_ADDR=:8080
